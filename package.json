{
    "dependencies": {
        "lit": "^2.2.1",
        "ts-lit-plugin": "^1.2.1"
    },
    "devDependencies": {
        "@web/dev-server": "^0.1.30",
        "@web/dev-server-esbuild": "^0.2.16",
        "typescript": "^4.6.3"
    },
    "scripts": {
        "build": "tsc",
        "start": "wds --node-resolve --open --watch"
    }
}